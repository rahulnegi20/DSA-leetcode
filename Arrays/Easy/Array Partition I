class Solution:
    def arrayPairSum(self, nums: List[int]) -> int:
        nums.sort()
        sum = 0
        for i in range(len(nums)-1):
            if i%2 ==0 :
                sum += nums[i]          
        return(sum)   
        
        
 #simple approach by doing sum of evrery even placed array after sorting       
