class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        new = [] #new list
        store =0
        for i in range(len(nums)):   
            store += nums[i]     #store saving new values one by one
            new.append(store)    #storing values in new[] list
        return new    
